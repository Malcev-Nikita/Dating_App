import { StyleSheet } from 'react-native';

const front_style = StyleSheet.create({
    button_container: { 
        flexDirection: 'row'
    },
    
    button: {
        backgroundColor: '#000'
    }
});

export default front_style